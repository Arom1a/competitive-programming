#include "bits/stdc++.h"

typedef long long ll;
using namespace std;

int main()
{
    // input
    int T;
    cin >> T;

    while (T--)
    {
        ll S;
        cin >> S;
        char ans = S % 10 == 0 ? 'E' : 'B';
        cout << ans << '\n';
    }
    // test
    // int N = ;
    // vector<int> c{};
    // expected:

    // answer below
}
