#include "bits/stdc++.h"

typedef long long ll;
using namespace std;

template <typename T>
void print_vector(const vector<T> &my_vec)
{
    for (const T &i : my_vec)
        cout << i << '\n';
    cout << endl;
}

int main()
{
    // input
    int N, Q;
    cin >> N >> Q;

    // test
    // vector<int> c{};
    // int Q = 1, N = c.size();
    // expected:

    // answer below
    while (Q--)
    {
        vector<int> c(N);
        for (int i = 0; i < N; i++)
        {
        }

        int ans = 0;

        cout << ans << '\n';
    }
}
